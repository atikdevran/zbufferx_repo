name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Find and Replace
        with:
          find: "ZBUFFERX"
          replace: "ZTRAFFIC"
          regex: false
