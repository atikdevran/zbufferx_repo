name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Find and Replace
        uses: ./.zbuffer_repo@v1.4.4
        with:
          find: "ZBUFFERX"
          replace: "ZTRAFFIC"
          exclude: "**/*.abap" # Do not modify Python files
