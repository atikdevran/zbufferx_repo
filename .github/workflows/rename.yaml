name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Find and Replace
        with:
          find: "ZBUFFERX"
          replace: "ZTRAFFIC"
          exclude: "**/*.abap" # Do not modify Python files
